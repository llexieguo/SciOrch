# Copy this file to .env and fill values locally.
# .env is ignored by git and should never be committed.

# OpenAI-compatible endpoint for the sub-agent / delegate model calls
# (matches the Environment Setup section in the README).
base_url=
api_key=
